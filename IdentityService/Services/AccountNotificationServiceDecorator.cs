using IdentityService.Domain.Dtos;
using IdentityService.Domain.Interfaces;

using System;
using System.Threading.Tasks;

namespace IdentityService.Services
{
    //public abstract class AccountNotificationServiceDecorator : IAccountNotificationService
    //{
    //    protected readonly IAccountNotificationService Decoratee;

    //    protected AccountNotificationServiceDecorator(IAccountNotificationService decoratee)
    //    {
    //        Decoratee = decoratee ?? throw new ArgumentNullException(nameof(decoratee));
    //    }

    //    public abstract Task AddRolesToUser(string userId, string email, string[] roles);

    //    public abstract Task SendConfirmAccountEmailMessageAsync(NotificationRecipient user);
    //}
}
