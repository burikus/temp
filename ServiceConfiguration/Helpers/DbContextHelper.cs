using Npgsql;
using System;

namespace ServiceConfiguration.Helpers
{
    //public static class DbContextHelper
    //{
    //    public static string CreateServiceDefaultDbConnectionString(string dbName)
    //    {
    //        var connectionStringBuilder = new NpgsqlConnectionStringBuilder
    //        {
    //            Host = Environment.GetEnvironmentVariable(string.Format(EnvironmentVariables.DefaultDbHost, dbName)),
    //            Database = Environment.GetEnvironmentVariable(string.Format(EnvironmentVariables.DefaultDbName, dbName)),
    //            Username = Environment.GetEnvironmentVariable(string.Format(EnvironmentVariables.DefaultDbUserName, dbName)),
    //            Password = Environment.GetEnvironmentVariable(string.Format(EnvironmentVariables.DefaultDbUserPassword, dbName)),
    //            SslMode = SslMode.Require,
    //        };
    //        return connectionStringBuilder.ConnectionString;
    //    }
    //}
}
